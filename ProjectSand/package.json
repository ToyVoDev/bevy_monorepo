{
  "name": "ProjectSand",
  "version": "1.0.0",
  "description": "Falling Sand Game in TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tsc": "tsc",
    "watch": "tsc --watch",
    "lint": "eslint ./**/*.{js,ts}",
    "lint:fix": "eslint ./**/*.{js,ts} --fix",
    "format": "prettier --write ./**/*.{js,ts,css,html}",
    "format:check": "prettier --check ./**/*.{js,ts,css,html}",
    "type-check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "matter-js": "0.19.0"
  },
  "devDependencies": {
    "@types/matter-js": "^0.20.2"
  }
}
